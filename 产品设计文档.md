# 制冷系统仿真软件产品设计文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 制冷系统仿真软件 |
| 当前版本 | MVP 产品设计 v0.1 |
| 文档状态 | 待评审 |
| 目标用户 | 制冷系统工程师、研发人员、技术支持人员 |
| 首版形态 | 本地 Web 应用 |
| 首版语言 | 中文 |
| 单位体系 | SI 单位制 |

## 2. 产品概述

### 2.1 产品定位

面向制冷系统工程研发场景，提供一个轻量、透明、可快速试算的制冷循环仿真工具。用户输入制冷剂和基础运行参数后，可以获得循环状态点、制冷量、压缩机功率、COP 以及 P-h 循环图，用于方案早期估算、参数对比和问题定位。

### 2.2 用户痛点

- 早期方案评估需要在多个表格、物性工具和手工计算之间切换。
- 计算结果缺少统一的状态点展示，难以快速检查参数是否合理。
- 总制冷量、压缩机功率和 COP 的计算口径不统一，容易误读。
- 复杂专业软件学习成本较高，不适合快速验证一个基础循环方案。

### 2.3 产品价值

- 将基础制冷循环计算集中到一个操作流程中。
- 使用真实制冷剂物性，提升工程估算的可信度。
- 用状态点表和 P-h 图解释结果，增强结果可追溯性。
- 通过参数调整快速比较不同运行条件下的性能变化。

## 3. MVP 目标与成功标准

### 3.1 MVP 目标

完成单级稳态蒸汽压缩制冷循环的可交互仿真，覆盖“输入参数 → 执行计算 → 查看指标和状态点 → 查看循环图”的最小闭环。

### 3.2 成功标准

- 新用户无需阅读长篇说明，即可完成一次基础仿真。
- 用户能够明确看到输入参数、计算结果和状态点之间的对应关系。
- 对同一组输入，结果可重复，且单位和计算口径清晰。
- 输入不合理或物性计算失败时，页面给出明确的错误原因和修改方向。
- 工程人员可以用结果进行早期方案比较，但不会误认为其等同于完整设备选型结果。

## 4. 用户与核心场景

### 4.1 主要用户

#### 工程研发人员

关注不同蒸发温度、冷凝温度、过热度和过冷度对制冷量及 COP 的影响，需要结果透明、单位明确、便于复核。

#### 技术支持人员

需要根据现场或客户提供的基础参数快速估算运行状态，并使用状态点和 P-h 图辅助解释系统表现。

### 4.2 核心使用场景

1. 用户选择 R134a、R410A 或 R32。
2. 输入蒸发温度、冷凝温度、过热度、过冷度、质量流量和压缩机等熵效率。
3. 点击“运行仿真”。
4. 查看制冷量、压缩机功率和 COP。
5. 查看四个循环状态点的压力、温度、焓、熵和干度。
6. 通过 P-h 图判断压缩、冷凝、节流和蒸发过程的状态变化。
7. 调整参数并重新运行，对比不同工况的结果。

## 5. 产品范围

### 5.1 MVP 包含

- 单级稳态蒸汽压缩制冷循环。
- R134a、R410A、R32 三种制冷剂。
- 蒸发温度、冷凝温度、过热度、过冷度、质量流量和压缩机等熵效率输入。
- 真实制冷剂物性计算。
- 制冷量、压缩机功率、COP、比制冷量和比压缩功计算。
- 四个状态点数据表。
- P-h 循环图。
- 输入校验、物性计算错误提示和结果单位说明。
- 本地运行和基础使用说明。

### 5.2 MVP 不包含

- 动态或瞬态仿真。
- 多级压缩、 economizer、喷气增焓或多回路系统。
- 管路压降、换热器效率、散热损失和环境换热细节。
- 压缩机排量、容积效率、转速和真实设备地图。
- 制冷剂充注量和系统泄漏分析。
- 自动设备选型、成本估算或性能保证。
- 用户账号、云端保存、协作和远程部署。
- 自动导出报告、批量计算和参数优化。

## 6. 功能需求

### FR-01 参数输入

用户可以在同一页面填写并确认一组完整的仿真输入：

| 参数 | 单位 | 说明 |
| --- | --- | --- |
| 制冷剂 | - | R134a、R410A、R32 |
| 蒸发温度 | °C | 蒸发侧饱和温度 |
| 冷凝温度 | °C | 冷凝侧饱和温度 |
| 过热度 | K | 压缩机入口相对蒸发饱和温度的过热量 |
| 过冷度 | K | 冷凝器出口相对冷凝饱和温度的过冷量 |
| 质量流量 | kg/s | 用于换算总制冷量和总压缩机功率 |
| 压缩机等熵效率 | % | 用于估算实际压缩过程 |

要求：

- 页面提供合理的初始值。
- 所有输入项显示单位。
- 用户可以在运行前修改任意参数。
- 重新运行时使用最新确认的参数。

### FR-02 输入校验

运行仿真前必须检查：

- 冷凝温度高于蒸发温度。
- 质量流量大于 0。
- 压缩机等熵效率大于 0 且不超过 100%。
- 过热度和过冷度不低于 0 K。
- 参数组合能够得到有效的物性状态。

错误提示需要指出具体参数和修改方向，不使用只有技术错误码的提示。

### FR-03 循环计算结果

成功运行后展示：

- 制冷量：kW。
- 压缩机功率：kW。
- COP：无量纲。
- 比制冷量：kJ/kg。
- 比压缩功：kJ/kg。
- 运行所使用的全部输入参数。

结果必须明确区分“总量”和“比量”，避免用户将单位制冷量误读为设备额定制冷量。

### FR-04 状态点结果

以表格展示四个状态点：

| 状态点 | 位置 | 必要数据 |
| --- | --- | --- |
| 1 | 压缩机入口 | 压力、温度、焓、熵、干度（如适用） |
| 2 | 压缩机出口 | 压力、温度、焓、熵、干度（如适用） |
| 3 | 冷凝器出口 | 压力、温度、焓、熵、干度（如适用） |
| 4 | 节流阀出口 | 压力、温度、焓、熵、干度（如适用） |

表格需要标明数值单位；对于不适用的干度显示“—”，不能伪造数值。

### FR-05 P-h 图

- 横轴为比焓，纵轴为压力。
- 按 1 → 2 → 3 → 4 → 1 顺序连接循环状态点。
- 状态点带编号和名称。
- 压力轴使用适合跨数量级观察的显示方式。
- 图表标题和坐标轴包含单位。

P-h 图用于解释循环过程，不在 MVP 阶段承诺完整饱和穹顶、等温线或等熵线绘制。

### FR-06 结果解释

页面需要展示简短的计算口径说明：

- 采用稳态循环假设。
- 节流过程按等焓处理。
- 未考虑管路压降和换热器压力损失。
- 总制冷量和总功率依赖用户输入的质量流量。
- 结果用于早期估算，不替代完整设备选型或实验验证。

## 7. 计算口径与边界

MVP 将循环抽象为四个基本过程：压缩、冷凝、节流和蒸发。

- 状态点 1：低压侧过热蒸气。
- 状态点 2：按压缩机等熵效率修正后的高压侧排气。
- 状态点 3：高压侧过冷液体。
- 状态点 4：经节流阀等焓降压后的低压侧状态。

主要输出按以下口径理解：

- 比制冷量 = 状态点 1 与状态点 4 的焓差。
- 比压缩功 = 状态点 2 与状态点 1 的焓差。
- 制冷量 = 质量流量 × 比制冷量。
- 压缩机功率 = 质量流量 × 比压缩功。
- COP = 比制冷量 ÷ 比压缩功。

当物性库无法为某组参数提供有效状态时，系统应阻止输出不完整结果，并提示用户调整参数。

## 8. 页面结构

### 8.1 参数区

位于页面左侧或顶部，包含制冷剂选择、全部输入参数、单位、默认值和运行按钮。

### 8.2 指标区

位于结果区顶部，优先展示制冷量、压缩机功率和 COP；比制冷量和比压缩功作为补充指标展示。

### 8.3 状态点区

使用可读表格展示四个状态点，并保持状态点编号与 P-h 图一致。

### 8.4 图表区

展示 P-h 图，并在图下方保留计算口径和模型限制说明。

### 8.5 错误区

错误应在用户操作附近或结果区域显著展示，包含：问题参数、失败原因和建议修改方式。

## 9. 非功能要求

- 本地启动后，用户可以通过浏览器访问应用。
- 常规参数组合的单次计算应在用户可接受的短时间内完成。
- 计算结果必须可重复，不依赖随机数或隐藏的状态。
- 输入、输出和图表单位必须统一为 SI 单位。
- 错误不能导致页面崩溃或展示上一次仿真结果冒充本次结果。
- 产品说明应明确模型限制，避免过度承诺计算精度。

## 10. MVP 验收标准

### 功能验收

- 可以选择三种制冷剂并完成基础仿真。
- 可以修改六类输入参数并重新计算。
- 成功结果包含三项核心指标、状态点表和 P-h 图。
- 状态点编号、表格和图表保持一致。
- 非法温度关系、非法效率、非法质量流量和物性失败均有明确提示。

### 结果验收

- 高压侧压力高于低压侧压力。
- 节流前后焓值符合等焓假设。
- 在有效常规工况下，制冷量、压缩机功率和 COP 为正值。
- 改变质量流量时，总制冷量和总功率随之变化，比指标和 COP 不因质量流量单独改变。
- 相同输入重复运行得到一致结果。

### 交付验收

- 提供本地安装和启动说明。
- 提供核心计算测试。
- 完成交付前测试验证。
- 每个独立功能以中文 Git commit 保存版本快照。

## 11. 后续迭代方向

在 MVP 通过评审和基础验证后，优先考虑：

1. 增加参数方案保存、复制和结果对比。
2. 增加饱和穹顶、等温线和等熵线等 P-h 图辅助信息。
3. 增加换热器效率、压力损失和压缩机排量模型。
4. 增加动态仿真和控制逻辑。
5. 增加结果导出和可追溯报告。
6. 扩展制冷剂和复杂循环类型。

## 12. 待评审问题

以下问题不阻塞 MVP 产品文档和首版原型，但应在进入正式工程化前确认：

- 是否需要将默认参数替换为目标设备或实验台的典型工况。
- 是否需要将 P-h 图扩展为带饱和边界的工程图。
- 是否需要支持多组工况的批量比较。
- 是否需要将产品从本地工具扩展为团队共享服务。
