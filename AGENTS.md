# 项目协作说明

## 版本管理

- 每次完成改动后，必须创建对应的 Git commit，保存可追踪、可回滚的版本快照。
- 每完成一个独立功能，至少创建一个独立 commit；不要把多个无关功能合并到同一次提交。
- 提交信息必须使用中文，并遵循“动词 + 修改内容”的格式，例如：`新增用户登录校验`。
- 提交前检查 `git status` 和 `git diff`，确认提交内容只包含本次改动。
- 每次创建 Git commit 后，必须立即将该 commit 同步推送到 GitHub 的 `origin/main`；推送完成后核对远端分支状态。

## 测试与交付

- 每次改动后，必须编写或更新与改动相关的测试。
- 交付前必须自行运行相关测试并检查结果。
- 发现测试失败或明显问题时，应先修复并重新验证，再报告完成。
- 如果改动不涉及可测试的程序行为（例如纯文档变更），应明确记录“无适用测试”，并完成相应的内容检查。

## 编码原则

- 编码前先明确目标、假设、验收标准和验证方式；遇到歧义时先说明，不擅自扩大范围。
- 优先采用能够直接解决当前需求的最简单实现，避免无必要的抽象、配置和扩展设计。
- 只修改需求涉及的代码，遵循现有项目风格，不顺手重构无关内容。
- 每一步完成后主动验证结果；最终交付时说明完成内容、测试结果和提交信息。
